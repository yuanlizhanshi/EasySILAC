% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{quality_control_SILAC}
\alias{quality_control_SILAC}
\title{Quality control for SILAC peptide data}
\usage{
quality_control_SILAC(
  se,
  assay_log2 = "log2_intensity",
  assay_raw = "raw_intensity",
  type_col = "type",
  time_col = "t",
  sample_col = "sample_name",
  peptide_col = "Peptide",
  heavy_label = "H",
  light_label = "L",
  intensity_q = 0.01,
  min_detected_timepoints_intensity = 3,
  min_detected_timepoints_ratio = 3,
  ratio_threshold = 0,
  min_spearman_rho = 0,
  norm_total = 1e+06,
  verbose = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment with raw/log2 intensity assays.}

\item{assay_log2}{Name of the log2 intensity assay.}

\item{assay_raw}{Name of the raw intensity assay.}

\item{type_col}{Column in `colData` marking heavy/light channel.}

\item{time_col}{Column in `colData` with the labeling time.}

\item{sample_col}{Column in `colData` with the sample name.}

\item{peptide_col}{Column in `rowData` with the peptide id.}

\item{heavy_label}{Label of the heavy channel in `type_col`.}

\item{light_label}{Label of the light channel in `type_col`.}

\item{intensity_q}{Intensity quantile cutoff (default 0.01).}

\item{min_detected_timepoints_intensity}{Minimum number of time points with
detected intensity (default 3).}

\item{min_detected_timepoints_ratio}{Minimum number of time points with a
valid H/(H+L) ratio (default 3).}

\item{ratio_threshold}{Minimum ratio value kept (default 0).}

\item{min_spearman_rho}{Minimum replicate Spearman correlation (default 0).}

\item{norm_total}{Normalization target for total intensity (default 1e6).}

\item{verbose}{Print progress messages.}
}
\value{
A list with filtered assays, QC statistics and kept peptides.
}
\description{
Filter a SILAC peptide-level SummarizedExperiment by intensity quantile,
number of detected time points, ratio range and replicate reproducibility
(Spearman), and normalize the total intensity. Returns a list with the
filtered assays and QC statistics.
}
