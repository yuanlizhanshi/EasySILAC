% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rust.R
\name{fit_silac_nonsteady}
\alias{fit_silac_nonsteady}
\title{Fit SILAC kinetics without the steady-state assumption}
\usage{
fit_silac_nonsteady(ratio, total, t, Peptide, min_points = 3, min_k = 1e-04)
}
\arguments{
\item{ratio}{Numeric vector of observed heavy ratios.}

\item{total}{Numeric vector of total intensities.}

\item{t}{Numeric vector of labeling times.}

\item{Peptide}{Peptide/protein identifier.}

\item{min_points}{Minimum number of distinct time points (default 3).}

\item{min_k}{Lower bound applied to the degradation rate (default 1e-4).}
}
\value{
A one-row tibble with fit results, or NULL if not fittable.
}
\description{
Non-steady-state fit: the degradation rate `k` comes from
`lm(log(old) ~ t)` with `old = total * (1 - ratio)`, and the synthesis rate
`s` from `nls(total ~ P0*exp(-k*(t-t0)) + (s/k)*(1-exp(-k*(t-t0))))`
(port algorithm, `s >= 0`). Reports `r2_old_decay` and total-fit error
metrics used for QC filtering.

By default the pure-R implementation is used; after `silac_use_rust(TRUE)`
the numerically identical Rust engine is used (see
[fit_silac_nonsteady_rust()]).
}
