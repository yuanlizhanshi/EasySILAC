% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hill.R
\name{fit_hill_labelling_efficiency}
\alias{fit_hill_labelling_efficiency}
\title{Fit global labeling efficiency with a Hill function}
\usage{
fit_hill_labelling_efficiency(
  full_ratio,
  sample_col = "sample",
  conc_col = "conc",
  time_col = "t",
  ratio_col = "mean_ratio",
  start = list(K = 0.5, h = 3, lambda = 0.25),
  lower = c(K = 1e-06, h = 0.1, lambda = 1e-06),
  upper = c(K = 10, h = 20, lambda = 10),
  conc_check = c(H100 = 1, H67 = 0.67, H33 = 0.33),
  conc_grid = seq(0.01, 1.2, length.out = 300),
  maxiter = 1000
)
}
\arguments{
\item{full_ratio}{A tibble with one row per sample and time point.}

\item{sample_col}{Column marking the labeling-efficiency sample (H100/H75/H50).}

\item{conc_col}{Column with the nominal heavy concentration.}

\item{time_col}{Column with the labeling time.}

\item{ratio_col}{Column with the (mean) heavy ratio.}

\item{start}{Named list of start values for K, h, lambda.}

\item{lower}{Named numeric vector of lower bounds.}

\item{upper}{Named numeric vector of upper bounds.}

\item{conc_check}{Named numeric vector of nominal concentrations.}

\item{conc_grid}{Grid of concentrations for evaluating E(x).}

\item{maxiter}{Maximum nlsLM iterations.}
}
\value{
A list with fit parameters (K, h, lambda), the efficiency function
  `E_fun`, corrected data frames, fit QC metrics and diagnostic plots.
}
\description{
Fits the labeling-efficiency curve `E(x) = x^h / (K^h + x^h)` jointly to
the heavy-ratio time courses of the H100/H75/H50 samples (minpack.lm),
returning K, h, the dilution rate lambda and corrected (collapsed) ratios
`c(t) = observed / E(x)`.
}
