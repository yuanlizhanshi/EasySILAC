% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rust.R
\name{fit_silac}
\alias{fit_silac}
\title{Fit SILAC heavy-ratio kinetics (steady state)}
\usage{
fit_silac(ratio, t, Peptide, total = NULL)
}
\arguments{
\item{ratio}{Numeric vector of observed heavy ratios H/(H+L).}

\item{t}{Numeric vector of labeling times.}

\item{Peptide}{Peptide/protein identifier (carried into the output).}

\item{total}{Optional numeric vector of total intensities (H+L).}
}
\value{
A one-row tibble with fit results, or NULL if the fit is not
  possible (too few points or non-finite rate).
}
\description{
Fits `log(1 - ratio) ~ t` by linear regression on all replicate points:
the heavy fraction follows `ratio(t) = 1 - C * exp(-k * t)`. Returns the
degradation rate `k`, half-life, intercept, standard errors and (when
`total` is given) the protein abundance `P0` and synthesis rate `k * P0`.

By default the pure-R implementation is used; after `silac_use_rust(TRUE)`
the numerically identical Rust engine is used (see [fit_silac_rust()]).
}
